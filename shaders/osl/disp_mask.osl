shader disp_mask(
    float dispAmount = 0.1,
    output float mask = 0
)
{
    //mask = (dispAmount > 0.1) ? 1.0 : 0.0; // sharp transition
    mask = smoothstep(0.09, 0.11, dispAmount); // soft transition around 0.1

}
